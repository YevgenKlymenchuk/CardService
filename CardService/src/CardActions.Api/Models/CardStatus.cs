namespace CardService.src.CardActions.Api.Models
{
    public enum CardStatus
    {
        Ordered,
        Inactive,
        Active,
        Restricted,
        Blocked,
        Expired,
        Closed
    }
}
